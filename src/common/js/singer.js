export default class Singer {
  constructor({id, name, picUrl, alias, musicSize, albumSize, topicPerson}) {
    this.id = id
    this.name = name
    this.picUrl = picUrl
    this.alias = alias
    this.musicSize = musicSize
    this.albumSize = albumSize
    this.topicPerson = topicPerson
  }
}
